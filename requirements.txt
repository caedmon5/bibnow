# Third-party dependencies for Bibnow v2

# For Zotero API calls (zotero_writer.py)
requests>=2.31.0

# For clipboard access on Linux (clipboard_loader.py)
pyperclip>=1.9.0

# Load .env files automatically instead of manual `export`
python-dotenv>=1.0.1
